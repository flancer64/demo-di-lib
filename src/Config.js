/**
 * This is just an object representing a hardcoded configuration. This module does not have any dependencies.
 */
const Sample_Lib_Config = {
    name: 'Sample Lib'
}

// This is a singleton defined on the ESM level, all imports use the same object.
export default Sample_Lib_Config;